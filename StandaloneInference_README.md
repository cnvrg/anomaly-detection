This blueprint deploys a pretrained anomaly detection endpoint. The model is trained on anomalous customers based on their attributes and subscriptions to services. 

User can proceed to call this API on new data points and get a response on whether the new data point is an anomaly or not.

Click on the use blueprint button and select the appropriate compute size to get started. 

